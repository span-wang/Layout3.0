# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第三阶段：AI 与体验 | `M3` 可发布 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH3-07` | 验证 M4 链路 | `P2` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH3-05` | 首次使用引导 | `暂缓` | 首次进入时有可关闭引导流程。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-09 | 范围调整 | 局部分栏快捷入口如果顺手扩成完整快捷键系统、右键菜单、拖拽调整边界、DOCX section 映射或分节系统，会把当前“多个块快速设为双栏/三栏”的小交互扩展成跨 UI、导出和排版的大改。 | 已完成 `PH2-19C-local-column-quick-actions-v1`：本步只增强现有 `columnSection` 的快捷入口，多选连续顶层块后可直接设为双栏或三栏；DOCX 局部分栏、拖拽边界、右键菜单、完整快捷键系统、对象独立跨栏和分节系统均未纳入。 |
| 2026-07-09 | 范围调整 | 文章结构模板如果加入内置模板、示例模板、模板市场、云同步、导入导出、结构化拖拽编辑或章节级局部重生成，会明显偏离用户当前要求的“模板由我自己定义、新增”，并把最小的 AI 输出结构约束扩成完整模板系统。 | 已完成 `PH3-01-ai-custom-structure-template-v1`：文章结构模板 V1 已按用户要求收口为本机自定义模板库，初始为空，只提供新增、编辑、删除和选择；模板只通过 localStorage 保存，选择后进入大纲和正文提示词；不提供内置模板、示例模板、云同步、导入导出、结构化编辑器或小红书模板。 |
| 2026-07-09 | 范围调整 | AI 教育内容生成从一次性生成正文改成“大纲审核后再生成正文”后，如果顺手扩展到小红书全链路、提示词模板中心、大纲结构化编辑、生成记录格式重做或知识库引用系统，会把一个明确的交互改造放大成 AI 工作流重构，影响 M4 收口稳定性。 | 已完成 `PH3-01-ai-outline-review-generation-v1`：教育内容四类生成已改为先生成可编辑大纲，用户审核后再依据该大纲生成最终 Markdown 正文；小红书链路、提示词模板中心、大纲结构化编辑、记录格式、分页和导出均未纳入。知识库启用时正文阶段复用大纲阶段同一批检索上下文，不扩成正文脚注或块级引用系统。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-09 | PH2-19C | 完成局部分栏快捷入口扩展 V1 | 模型参数扩展/store 接线/多选浮动条双栏三栏入口/定向测试/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需右键菜单、快捷键、拖拽调整区段边界、DOCX 局部分栏或完整分节系统，再按对应任务另拆小步。 |
| 2026-07-09 | PH2-19C | 确认局部分栏快捷入口扩展 V1 边界 | 用户确认/边界回写/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现模型参数、store 动作、画布多选浮动工具条双栏/三栏快捷入口和定向回归。 |
| 2026-07-09 | PH3-01 | 完成 AI 自定义文章结构模板 V1 | 模板库状态新增/模板管理 UI/提示词接入/定向测试/类型检查/构建验证/进度同步 | 后续如需模板导入导出、工作区模板库、结构化模板编辑器、模板与生成记录联动或小红书结构模板，再另拆 AI 体验增强小步。 |
| 2026-07-09 | PH3-01 | 确认 AI 自定义文章结构模板 V1 边界 | 用户确认/边界回写/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现本机自定义结构模板状态、AI 生成设置区模板管理 UI、提示词接入和定向回归。 |
| 2026-07-09 | PH3-01 | 完成 AI 大纲审核后生成正文 V1 | AI 生成链路改造/大纲审核 UI/提示词方法新增/定向测试/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需大纲结构化编辑、局部重生成、保存大纲审核痕迹或改造小红书链路，再另拆 AI 体验增强小步。 |
| 2026-07-09 | PH3-01 | 确认 AI 大纲审核后生成正文 V1 边界 | 用户确认/边界回写/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现教育内容先大纲审核再生成正文的前端链路、提示词方法和定向回归。 |
| 2026-07-08 | PH2-14 | 收尾段落样式同类块应用与 Word 式行距 V1 | 实现校准/导出口径补齐/定向测试/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；PH2-14 本小步完成后，下一步可回到 `PH3-12` 个人知识库真实联调收尾或按用户确认进入 `PH3-07` M4 链路验证。 |
| 2026-07-08 | PH3-05 | 暂缓首次使用引导 | 用户决策/任务状态调整/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续除非用户重新确认恢复 `PH3-05`，否则下一步继续从 `PH3-12` 个人知识库真实联调收尾或 `PH3-07` M4 链路验证中选择。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
