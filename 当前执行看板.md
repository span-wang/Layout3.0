# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-21F` | DOCX 方程组大括号兼容 V1 | `P1` | `进行中` | DOCX 公式导出支持二元一次方程组和分段函数这类“左大括号 + 多行”结构；至少覆盖 `\begin{cases}...\end{cases}`、`\left\{\begin{aligned}...\end{aligned}\right.` 与 `\left\{\begin{array}...\end{array}\right.`；Word/WPS 中应优先输出可伸缩的大括号和多行 Office Math 结构，而不是整条退回 LaTeX 源码。 |
| `PH2-24A` | 答案显示与试卷解析位置扩展 V1 | `P1` | `进行中` | 自定义语义名称包含 `答案` 或 `解析` 的块，可与内置 `答案 / 解析` 一样参与显示/隐藏与文末统一；`blockquote / 引用容器` 内部的答案解析子块也可参与文末统一与隐藏；画布预览、分页、HTML/PDF、DOCX 和 `.layout` 保存恢复口径一致。 |
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-07 | 范围调整 | 个人知识库接入依赖本机外部服务状态。当前 `RAGFlow` 预期端口 `9380`、`Open Notebook` 预期端口 `8502 / 5055` 均未监听，Docker 也未处于可用状态；如果本步直接把功能边界扩成“必须完成真实双服务联调和完整生成闭环”，会把 UI、连接器开发和本地环境排障耦合在一起，导致最小可验收版本失焦。 | 已完成第一轮实现：左侧“个人知识库”面板、RAGFlow / Open Notebook 连接配置与探活、RAGFlow 数据集读取和检索测试入口、以及 AI 生成结合 RAGFlow 上下文的代码链路都已落地，并已通过 `npm run typecheck` 与 `npm run build`。当前继续保留本风险，原因是本机 `9380 / 8502 / 5055` 端口仍未监听、Docker 也未恢复，尚未完成真实双服务联调；待本机服务启动后，再补齐实机验证与更深层的 Open Notebook 检索能力。 |
| 2026-07-07 | 范围调整 | DOCX 方程组大括号兼容如果一步从 `cases` 扩成完整二维公式引擎，会迅速扩张到通用矩阵、`align` / `align*`、多对齐点、行列式和更多 Office Math 专用结构；当前 `docx` 封装没有现成的方程数组或省略定界符高级对象，直接扩大会明显提高实现和回归成本。 | 已完成 `PH2-21-docx-equation-system-brace-v1`：`src/services/docxMath.ts` 已补齐 `cases`、左大括号包裹的 `aligned / array`，以及对应的多行方程数组和省略定界符结构，让二元一次方程组和分段函数不再整条退回源码；`src/services/exportDocx.test.ts` 已补 XML 回归，定向测试、typecheck 与 build 均已通过。通用矩阵、完整 `align` 语义、`\binom`、行列式和更复杂二维公式对象继续后续单拆。 |
| 2026-07-07 | 范围调整 | 图片复杂绕排如果一步追平 Word 全量，会同时牵动透明像素轮廓分析、逐行真实测量分页、跨页锚点、DOCX 浮动对象、图层顺序和键盘微调，容易让当前“任意方向拖动 + 正文持续绕排”失去可验收边界。 | 已完成 `PH2-10A-free-drag-complex-wrap-v1`：当前已在现有 `wrapMode / wrapSide / offsetX / offsetY` 基础上补齐页面级浮动矩形绕排最小闭环，覆盖四周型/紧密型图片在当前页内自由拖动、拖动跨中线自动换侧、普通文本类块持续避让，以及画布预览 / 分页估算 / HTML/PDF 的统一口径；透明轮廓、DOCX 浮动对象、跨页自由定位、图层顺序和键盘微调继续后续另拆。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-07 | PH3-12 | 收口个人知识库面板文案 | 界面文案精简/描述性文字移除/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需继续压缩其他面板文案，可按同样方式继续收口。 |
| 2026-07-07 | PH3-12 | 完成个人知识库接入 V1 第一轮实现 | 左侧菜单接入/知识库配置存储/RAGFlow 连接器/AI 生成链路接线/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；如果本机 Docker / 知识库服务恢复，再继续做 `PH3-12` 的真实接口联调和端到端生成验证。 |
| 2026-07-07 | PH3-12 | 确认个人知识库接入 V1 边界 | 用户确认开工/范围收口/风险登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现左侧“个人知识库”菜单、知识库面板、主进程知识库请求与 RAGFlow 检索接入。 |
| 2026-07-07 | PH2-10A | 完成图片自由拖动与复杂绕排 V1 | 画布拖动预览/隐藏测量层共享正文流/导出样式同步/类型检查/定向验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续如需继续贴近 Word，可优先拆透明轮廓紧密绕排、DOCX 浮动对象或跨页自由定位。 |
| 2026-07-07 | PH2-10A | 确认图片自由拖动与复杂绕排 V1 边界 | 用户确认开工/范围收口/风险登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现图片自由拖动、页面级浮动绕排、分页估算和 HTML/PDF 导出联动。 |
| 2026-07-06 | PH2-07-semantic-block-template | 完成语义块模板 V1 | 文档模型扩展/对象属性接线/预览导出同步/定向回归/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续如需扩到更多块类型、允许块模板改变高度，或开放块模板编辑器，再按 `PH2-07` 后续小步继续拆。 |
| 2026-07-06 | PH2-07-semantic-block-template | 确认语义块模板 V1 边界 | 用户确认开工/设计决策收口/边界登记/风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现块模板文档模型字段、默认模板解析、对象属性面板、预览/导出接线与最小验证。 |
| 2026-07-06 | PH2-05B | 完成分液漏斗与滴定管容器扩展 V1 | 特殊容器状态模型扩展/动态 SVG 渲染/属性区阀门开关/定向测试/类型检查/构建验证/进度同步 | 本步已完成；后续可继续推进特殊容器连接点扩容、更多可编辑器材、动画放液或组合成组/解组。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
