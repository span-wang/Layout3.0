# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第三阶段：AI 与体验 | `M3` 可发布 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH3-07` | 验证 M4 链路 | `P2` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH3-05` | 首次使用引导 | `暂缓` | 首次进入时有可关闭引导流程。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-08 | 范围调整 | 用户已明确要求当前不做 `PH3-05` 首次使用引导。如果继续把它保留为待确认下一步，后续看板会持续推荐一个已经被用户排除的方向，容易干扰 `PH3-12` 个人知识库真实联调和 `PH3-07` M4 链路验证的推进顺序。 | 已将 `PH3-05` 状态调整为 `暂缓`，当前不再设计或实现首次使用引导。后续除非用户重新确认恢复该任务，否则第三阶段优先继续处理 `PH3-12` 个人知识库真实联调收尾或 `PH3-07` M4 链路验证；更完整帮助中心与快捷键说明仍可在 `PH4-05` 文档与帮助任务中单独推进。 |
| 2026-07-08 | 范围调整 | 段落样式如果直接照搬“把当前块样式复制到所有同类块”的做法，或一步引入真正的 Word 倍数行距数据模型，会同时牵动 `blockStyleOverrides` 污染、样式优先级、分页测量签名、HTML/PDF 导出和 DOCX 行距解释，极易把当前最需要的“先能按同类块分别应用、并提供常用行距选项”最小闭环拖成大改。 | 已按最小闭环完成本步：同类块段落样式规则已收口为写入现有 `LayoutDocument.styles.blockStyles`，当前块 `blockStyleOverrides` 继续保留更高优先级；Word 风格行距选项 V1 已换算到现有 `lineHeight` 数值写回，没有在本步引入新的倍数行距字段。当前已用 `quickBlockStyle.test.ts` 覆盖规则写入、递归合并、局部覆盖优先与 HTML/PDF 直接消费，并用 `exportDocx.test.ts` 覆盖 DOCX 对同类块对齐、行距、段前距和段后距的消费。后续只有在这个基础口径被验证稳定后，才继续考虑独立升级为更完整的行距模型。 |
| 2026-07-08 | 阻塞 | 当前本机 `RAGFlow` 服务虽然已启动，但应用里保存的 `API Key` 在真实接口上返回 `401 Unauthorized` 和 `Authentication error: API key is invalid!`。这会直接阻塞 `PH3-12` 的数据集读取、检索测试和 AI 生成带知识库上下文的端到端验收；如果不把这个阻塞点单独登记，后续容易把“凭证已失效”误判为前台代码没接通。 | 已完成真实联调定位：`Open Notebook` API 探活正常，`RAGFlow` 服务端口可达，但当前本机保存的 `RAGFlow API Key` 已失效。本轮已先补 `KnowledgeBaseService` 的中文认证失败诊断与定向测试，前台后续会明确提示用户检查或重新生成 API Key；待用户更新有效密钥后，再继续完成 `PH3-12` 的真实端到端收尾。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-08 | PH2-14 | 收尾段落样式同类块应用与 Word 式行距 V1 | 实现校准/导出口径补齐/定向测试/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；PH2-14 本小步完成后，下一步可回到 `PH3-12` 个人知识库真实联调收尾或按用户确认进入 `PH3-07` M4 链路验证。 |
| 2026-07-08 | PH3-05 | 暂缓首次使用引导 | 用户决策/任务状态调整/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续除非用户重新确认恢复 `PH3-05`，否则下一步继续从 `PH3-12` 个人知识库真实联调收尾或 `PH3-07` M4 链路验证中选择。 |
| 2026-07-08 | PH2-14 | 完成段落样式同类块应用与 Word 式行距 V1 | 样式规则建模/右侧面板接线/分页与导出统一消费/定向测试/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需真正升级为独立的倍数行距数据模型，再按用户确认另拆样式系统增强小步。 |
| 2026-07-08 | PH2-14 | 确认段落样式同类块应用与 Word 式行距 V1 边界 | 用户确认/范围收口/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现段落样式同类块规则、Word 式行距入口和导出 / 分页统一消费。 |
| 2026-07-08 | PH3-02 | 完成命令面板 V1 | 新组件开发/快捷键接线/命令复用/样式补齐/构建验证/进度同步 | 第三阶段可继续回到 `PH3-12` 个人知识库真实联调，或在你确认后进入 `PH3-05` 首次使用引导。 |
| 2026-07-08 | PH3-02 | 确认命令面板 V1 边界 | 用户确认/范围收口/风险登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现命令面板浮层、全局快捷键、命令检索和最小命令集。 |
| 2026-07-08 | PH2-01 / PH2-21F / PH2-24A | 完成第二阶段 M2 收口与状态校正 | 任务状态收口/阶段里程碑同步/展示层重建/一致性校验 | 第二阶段 `M2` 已完成；下一步按 AGENTS 约定先确认第三阶段小步边界，可优先从 `PH3-02` 命令面板 V1 或 `PH3-12` 个人知识库真实联调中二选一。 |
| 2026-07-08 | PH2-01 / PH2-21F / PH2-24A | 确认第二阶段 M2 收口与状态校正边界 | 用户确认/边界回写/进度同步 | 开始同步 `PH2-01`、`PH2-21F`、`PH2-24A` 与 `phase-2 / M2` 的状态，并重新生成展示层文档。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
