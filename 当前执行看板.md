# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M4` AI 就绪 | 进行中 / 进行中 |

## 正在进行

当前没有显式标记为 `进行中` 的任务。

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-28 | 缺陷 | 手绘主题的标题装饰存在额外占位高度和缩进，但当前分页测量/估算缺少统一的主题装饰占位契约，后续更多主题如果继续用 CSS 装饰也可能让分页结果低估块高或切页不稳定。 | 已完成 `PH2-09-theme-decoration-measure-contract-v1`：主题装饰占位已进入 `ThemeLayoutMetrics` 和 `ResolvedStyleContract`，手绘主题 H1/H2/H3 占高装饰通过统一 CSS 变量进入画布预览、隐藏测量层和 HTML/PDF 导出；分页估算和分页测试算法1会计入这些占位，并让测量缓存随主题布局指标变化失效。后续新增占高装饰主题必须复用该契约。 |
| 2026-06-28 | 缺陷 | 当前块排版自定义预设虽然会随 `.layout` 保存恢复，但切换到其他 Markdown、新建空白文档或其他文件时，右侧「块排版」面板会重置为默认样式设置，导致用户刚保存的自定义预设列表消失，容易误判为“没有做持久化”。 | 已完成 `PH2-15-cross-file-preset-library-fix-v1` 小修复：在保留 `.layout` 文档内持久化的同时，新增本机用户级块排版预设库；打开 Markdown、打开 `.layout`、新建空白文档、切换文件和恢复草稿时统一合并内置预设、当前文档预设和本机预设库，确保用户自定义预设跨文件仍可见、可复用，但不自动改动当前文件已应用的排版参数。已通过 `typecheck`、`smoke:m2` 和 `build` 验证。 |
| 2026-06-28 | 缺陷 | 当前预览页与 PDF 导出页在页面底端存在可见偏差，尤其带表格的页面更明显；若预览和导出继续使用不同的页面高度与表格行高口径，用户会误以为分页结果不可靠。 | 已完成第二轮收口修复：导出页现已显式使用与预览一致的固定页面高度和正文 `overflow: hidden` 裁切；表格导出改为复用 `.table-shell / .preview-table / .table-cell-content / colgroup` 结构，并统一 `table-layout`、列宽、默认行高、表头行高和单元格换行口径；同时修复了导出表格在过滤后使用错误列索引取宽高的问题，并补齐 H3 默认字号 fallback 差异。已通过 `typecheck`、`smoke:m2`、`build` 和进度校验验证。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-28 | PH2-02 / PH2-03 | 完成第二阶段 M2 里程碑正式收尾 | 收尾验收/任务卡更新/里程碑关闭 | 第二阶段 M2 里程碑已正式关闭。后续可按优先级推进第三阶段 AI 相关任务（PH3-01 及后续），或根据用户需求继续深化第二阶段相关功能的细化工作。 |
| 2026-06-28 | PH2-19 | 完成标题参与分栏开关 V1 | 功能增强/样式契约/预览导出分页口径统一/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续如需更细标题层级跨栏控制、局部分栏或对象跨栏，继续按 `PH2-19` / `PH2-10` 后续小步另行确认。 |
| 2026-06-28 | PH2-09 | 完成分页测试算法1分栏回归修复（列表/表格） | 分页缺陷修复/实时高度复算/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户可切换分页测试算法1重新检查截图中的列表/表格分页，不应再出现后续页面只放一项或一行的异常。 |
| 2026-06-28 | PH2-09 | 确认分页测试算法1分栏回归修复边界（列表/表格） | 缺陷确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始修复 `estimatedMaxFill.ts` 中列表/表格拆分可用高度复用旧值的问题，并补充 `smoke:m2` 回归验证。 |
| 2026-06-28 | PH2-19 | 确认标题参与分栏开关 V1 边界 | 需求确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现标题参与分栏开关的数据结构、右侧面板入口、预览/导出跨栏规则和分页估算判断。 |
| 2026-06-28 | PH2-19 | 完成分栏断点 V1 | 结构块扩展/插入入口/分页语义/导出对齐/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续如需继续深化分栏，可再拆局部章节分栏、图片/表格/公式跨栏或更精确的多栏真实测量分页。 |
| 2026-06-28 | PH2-19 | 确认分栏断点 V1 边界 | 需求确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现分栏断点结构块、插入入口、预览/导出标记和分页估算中的换栏语义。 |
| 2026-06-28 | PH2-19 | 完成双栏挤出第三栏修复 V1 | 分页缺陷修复/栏位推进口径收口/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续如需继续深化分栏，可再拆分栏断点、局部分栏或更精确的多栏真实测量分页。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
