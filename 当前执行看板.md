# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第三阶段：AI 与体验 | `M3` 可发布 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |
| `PH3-13` | 资料入库中心与资料生命周期治理 | `P2` | `进行中` | 按 `RAGFLOW_input完整开发方案.md` 完成统一登记、版本与去重、元数据人工确认、持久处理、pending 质量门禁、精确 ID 排序前门禁与 SQLite 返回后二次校验、发布回滚、历史迁移和长期运营；阶段 0 必须先通过 Electron 32 SQLite 与隔离 RAGFlow 合同验证。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH3-07` | 验证 M4 链路 | `P2` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH3-05` | 首次使用引导 | `暂缓` | 首次进入时有可关闭引导流程。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-19 | 范围调整 | 教育内容生成从强制大纲审核改为可选模式后，如果未勾选时简单复用旧的一次生成入口，用户已选择的文章结构模板可能不会进入正文提示词；知识库来源也可能因绕开大纲阶段而丢失。若同时扩展到偏好持久化、小红书链路或大纲版本管理，又会把当前最小交互调整扩大成 AI 工作流重构。 | 已完成 `PH3-01-ai-optional-outline-generation-v1`：四类教育内容新增默认不勾选的“生成大纲”复选框；勾选时保留现有两步流程，未勾选时直接生成正文。直接正文链路已显式继承文章结构模板，并在启用知识库时执行一次检索、复用来源到结果与生成记录；小红书、偏好持久化、大纲版本和分页导出均未纳入。 |
| 2026-07-11 | 产品逻辑调整 | 当前质量门把“资料用途是否符合预期”和“索引是否可安全检索”混为一层，并要求用户手填 3～5 条问题/证据且全部命中。短资料、词汇表或单主题讲义会被迫凑题，少量人工问题也无法证明全文质量，容易造成无意义阻断或错误的通过承诺。 | 已完成 `PH3-13C3-automatic-index-health-gate-v1`：资料用途继续由上传时的人工元数据/内容确认承担；质量门只自动抽取 1～3 个定位覆盖样本，核验其 Top 10 检回和零泄漏。1～3 个结构单元抽 1 个、4～6 个抽 2 个、7 个及以上抽 3 个；抽样全部通过只表述为索引健康，不表述为全文语义质量保证。开始检查只提交 itemId，旧的 3～5 条持久化与发布复核限制已同步移除。156/156 登记库回归、类型检查和生产构建通过；全文评估、语义评分和批量运营后续归属 PH3-13G/H。 |
| 2026-07-11 | 体验与质量 | 如果为免除手填而直接自动提交质量运行、使用外部 AI 自由编造问题/证据，或从正文中挑选非唯一片段，会让质量门在无人工确认或无可审计来源的情况下误通过；反过来，若只给空模板，用户仍需逐项复制原文，体验问题没有解决。 | 该风险中的历史“一键建议回填”方案已由 RISK-108 和 `PH3-13C3-automatic-index-health-gate-v1` 替代：系统不再显示或接收人工问题/证据，而是从已核验工件自动抽取 1～3 个唯一、可定位样本。用户仍需主动点击开始，系统不调用外部 AI；抽样只验证索引健康与范围安全，不替代资料用途判断。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-19 | PH3-01 | 完成 AI 可选大纲生成 V1 | 生成流程分支调整/直接正文提示词补强/定向测试/类型检查/生产构建/进度收口 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；用户可在当前桌面应用重载后人工确认默认未勾选、勾选时按钮切换为“生成大纲”。 |
| 2026-07-19 | PH3-01 | 确认 AI 可选大纲生成 V1 边界 | 用户确认/边界回写/范围调整风险登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后实现教育内容可选大纲开关、直接正文链路和定向回归。 |
| 2026-07-11 | PH3-13 | 完成自动索引健康检查重构 | 质量门实现替换/回归验证/进度收口 | 重启桌面应用，在资料进入质量检查阶段时直接点击“开始质量检查”。若自动样本无法检回或发现范围泄漏，按质量报告处理，不需要手填问题。 |
| 2026-07-11 | PH3-13 | 确认自动索引健康检查重构边界 | 质量门产品逻辑澄清/用户确认/范围替换与风险回写 | 执行进度生成与一致性校验；随后移除人工问题/建议表单，改造 QualityGateService 自动 1～3 样本建档并补齐回归。 |
| 2026-07-11 | PH3-13 | 完成质量问题与来源证据一键生成 | 本地唯一证据建议/Main 服务与受控 IPC/preload/store/中文表单回填/全量回归/构建验证/进度同步 | 重启当前桌面应用，在质量门禁区点击“一键生成核验项”；检查可编辑问题和原文证据后，由用户主动点击“开始质量检查”。若正文无法给出 3 条唯一证据，按中文提示手动补充，不绕过质量门。 |
| 2026-07-11 | PH3-13 | 确认质量问题与来源证据一键生成边界 | 用户体验反馈/方案确认/边界与质量风险回写 | 执行进度生成与一致性校验；随后实现 Main 本地建议服务、受控 IPC/preload/store 和中文表单一键回填，并补齐定向回归。 |
| 2026-07-11 | PH3-13 | 完成 RAGFlow 空 metadata 兼容性修复 | 受控 metadata 构造修复/客户端出站失败关闭/登记库全量回归/类型检查/生产构建/进度同步 | 重启当前桌面应用后，在资料入库中心对失败资料点击“人工重试”；确认它复用同名远端文档并进入解析等待/质量门。若仍失败，只记录新的稳定错误类别与安全摘要，不绕过质量门或删除远端文档。 |
| 2026-07-11 | PH3-13 | 确认 RAGFlow 空 metadata 兼容性修复边界 | 真实失败审计诊断/用户确认/修复边界与风险回写 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后仅修复 Main metadata 出站构造、客户端 null 保护与定向回归，不对真实 RAGFlow 发起自动请求。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
