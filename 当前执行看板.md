# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-21A` | DOCX 导出分页策略修正 V1 | `P1` | `进行中` | DOCX 导出不再按预览每页强制创建 `nextPage` section；同页设置一致时按连续正文流导出，由 Word/WPS 自然填满页面；用户显式插入的分页符仍保留为真实分页符；不破坏现有 PDF 导出与 DOCX 基础能力。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-06 | 范围调整 | 语义块模板如果一步扩到所有块类型、允许改变块高度，或与现有文档模板/主题同权叠加，会同时牵动分页高度、标题主题装饰、列表/引用外壳冲突和旧 `.layout` 文件迁移脏写风险。 | 已完成 `PH2-07-semantic-block-template-v1`：当前已把风险收口为最小闭环并落地代码，只支持 `paragraph / heading / list / blockquote` 4 类块；内置 `默认语义框 / 侧边强调 / 浅色卡片 / 警示框` 4 种不改高度的块模板；块模板只接管外壳样式，普通块和语义块共用同一套模板库；旧文件只在运行时按“语义角色 -> 默认块模板”解析，不批量回填新字段；只有用户手动改块模板或触发语义前备份时才写入块级状态。后续若继续扩到更多块类型、可改高度模板或模板编辑器，需另拆小步并重新评估分页与主题冲突。 |
| 2026-07-06 | 范围调整 | 当前导出主链路围绕 `exportHtml + printToPDF` 建立，导出 HTML 大量依赖 `layout-asset://` 本地资源协议、主题背景纹理、SVG 装饰和复杂 CSS；若在同一步强行追求 DOCX 与 PDF 1:1 保真，会把范围放大到资源协议改造、图片嵌入、公式渲染、多栏分页和 Word 专属版式映射等多条链路。 | 已完成 `PH2-21-docx-export-v1` 最小闭环：工具栏已新增 `导出 DOCX`，renderer / preload / 主进程已形成独立 DOCX 保存链路；已覆盖标题、段落、列表、表格、图片、代码块、引用块、分页符、基础页尺寸页边距、基础页眉页脚与页码；本地图片会读取真实资源嵌入，SVG 会先转 PNG 再嵌入，避免 `layout-asset://` 直接泄漏到 DOCX。`npm run typecheck`、`npm run build` 与最小 DOCX 烟测已通过。2026-07-06 用户进一步反馈：当前问题不是表格，而是预览里能填满的内容在 DOCX 中因为“预览每页一个 section + nextPage”被提前强制翻页，导致页面底部留下大块空白。该问题已通过 `PH2-21-docx-pagination-flow-fix-v1` 完成返修：同页设置一致时，DOCX 现已改为连续正文流导出，不再按预览每页硬切 `nextPage` section；用户显式 `pageBreak` 仍会输出为真实分页符。连续流模式下 `{本页标题}` 不再承诺与预览页逐页 1:1，对应更高保真的页眉语义留待后续深化。主题纹理、SVG 装饰、多栏、复杂图片环绕/裁剪和公式高保真 1:1 继续留在后续任务。 |
| 2026-07-06 | 范围调整 | 导管连接点吸附若一步扩到自动避障布线、导管部件互吸附和成组约束，会把当前最小导管交互放大成完整连线编辑器；同时，吸附关系必须和容器移动、缩放、旋转保持一致，否则会出现导管端点漂移或导出位置错误。 | 已完成 `PH2-05B-chemistry-connector-snap-v1`：当前只覆盖导管线端点到 10 类容器与单孔塞 / 双孔塞连接点的吸附；吸附后端点会跟随目标部件移动、缩放、旋转同步更新，手动整体拖动、缩放或旋转导管线时会主动解除吸附。自动避障布线、导管 SVG 部件互吸附、自动对齐、成组/解组和模板另存继续留在后续拆步。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-06 | PH2-07-semantic-block-template | 完成语义块模板 V1 | 文档模型扩展/对象属性接线/预览导出同步/定向回归/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续如需扩到更多块类型、允许块模板改变高度，或开放块模板编辑器，再按 `PH2-07` 后续小步继续拆。 |
| 2026-07-06 | PH2-07-semantic-block-template | 确认语义块模板 V1 边界 | 用户确认开工/设计决策收口/边界登记/风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现块模板文档模型字段、默认模板解析、对象属性面板、预览/导出接线与最小验证。 |
| 2026-07-06 | PH2-05B | 完成分液漏斗与滴定管容器扩展 V1 | 特殊容器状态模型扩展/动态 SVG 渲染/属性区阀门开关/定向测试/类型检查/构建验证/进度同步 | 本步已完成；后续可继续推进特殊容器连接点扩容、更多可编辑器材、动画放液或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认分液漏斗与滴定管容器扩展 V1 边界 | 用户确认开工/特殊容器范围登记/风险范围收口 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展容器状态模型、分液漏斗/滴定管动态 SVG 和右侧属性区的阀门开关。 |
| 2026-07-06 | PH2-05B | 完成化学导管连接点吸附 V1 | 连接点元数据扩展/导管吸附关系建模/端点手柄与高亮/类型检查/生产构建 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续可继续推进自动避障布线、导管部件互吸附、自动对齐或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认化学导管连接点吸附 V1 边界 | 用户确认开工/连接点范围登记/吸附一致性风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展连接点元数据、实现导管线端点吸附与跟随更新。 |
| 2026-07-06 | PH2-05B | 完成化学容器编辑 V1 | 容器元数据扩展/动态 SVG 渲染/属性区接入/类型检查/生产构建 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续可继续推进导管连接点吸附、自动对齐或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认化学容器编辑 V1 边界 | 用户确认开工/容器属性范围登记/动态渲染风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展可编辑容器元数据、实现组合设计器属性区与动态容器渲染。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
