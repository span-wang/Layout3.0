# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

当前没有显式标记为 `进行中` 的任务。

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH2-09` | 精细分页算法（二次推进） | `暂缓` | 基于结构化文档模型建立可切换的分页算法注册框架，并在此基础上继续推进孤寡行、表格跨页、表头重复与真实测量分页。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-27 | 缺陷 | 桌面端多个输入框存在明显输入等待，部分右侧属性输入每个字符都会写回 `LayoutDocument`，进而触发撤销栈、分页重算和隐藏测量层，导致普通输入也被重模型更新拖慢。 | 已完成首轮修复：画布富文本编辑和右侧图片/公式属性输入已改为本地草稿或非受控输入，确认后再写回模型；`CanvasPane` 已增加记忆化隔离。若仍存在全局卡顿，再另拆画布渲染隔离深化或隐藏测量层降频任务。 |
| 2026-06-27 | 返工 | AI 请求继续从 renderer 直接发起时仍会受浏览器 CORS 限制影响，前端代理或公开代理方案也难以可靠转发 `Authorization` / `x-api-key` 等鉴权头，用户实际配置后仍可能出现 `failed to fetch`。 | 已完成修复：AI HTTP 请求已迁移到 Electron 主进程 IPC 执行，renderer 只负责构造业务请求和读取返回结果；AI 设置页已删除代理 URL，AiConfig 已删除 proxyUrl 字段，主进程支持按 requestId 取消正在执行的请求。 |
| 2026-06-27 | 缺陷 | AI 基础结构代码审核发现生成内容写回模型字段不完整、停止按钮未取消网络请求、代理配置未统一生效、Anthropic 流式响应未解析、检查前文档内容转换失真，以及部分按钮为可见空功能。 | 已完成修复：AI 生成内容通过完整 LayoutDocument/TextRun 契约写入并进入撤销栈；生成、优化、检查已接入 AbortController；请求统一走 AiService 代理 URL 规则；Anthropic 流式响应已支持 `delta.text`；检查前文档转换已带出表格、图片 alt 与列表项；暂未落地按钮已改为禁用提示；API Key 文案已改为本机 localStorage 保存说明。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-27 | PH2-12 | 完成桌面端输入响应修复 V1 | 修复/验证/文档同步 | 用户在桌面端重新测试画布原位编辑、右侧图片属性、公式源码、左侧搜索和 AI 设置输入响应；若仍有明显等待，再单独拆 `CanvasPane` 渲染隔离深化或隐藏测量层降频任务。 |
| 2026-06-27 | PH3-01 | 完成 AI 生成记录本地文件存储 | 新增/验证/文档同步 | 用户可完成一次 AI 生成后，在工作区 `AI生成记录/records.json` 查看落盘内容，并通过左侧“AI生成记录”复用历史结果；后续若需要搜索、标签、收藏、导出或优化/检查历史，继续归入 PH3-07 或单独 AI 体验增强任务。 |
| 2026-06-27 | PH2-12 | 确认桌面端输入响应修复边界 | 方案确认/风险登记/准备修复 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物，然后开始实现输入本地草稿与确认提交修复。 |
| 2026-06-27 | PH3-01 | 确认 AI 生成记录本地文件存储边界 | 方案确认/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后实现 AI 生成记录 IPC、前端服务、左侧菜单展示和生成完成自动落盘。 |
| 2026-06-27 | PH3-01 | 完成 AI Markdown 结构化插入修复 | 修复/验证/文档同步 | 用户可重新生成一段 Markdown 内容并点击“插入当前文档”，确认画布中显示结构化标题、正文、表格、公式和列表；后续若需要优化选区替换或插入前结构预览，继续归入 PH3-01 增强或 PH3-07 验收增强。 |
| 2026-06-27 | PH3-01 | 确认 AI Markdown 结构化插入修复边界 | 方案确认/准备修复 | 执行 `npm run progress:build` 同步展示层产物，然后实现 AI Markdown 结构化插入并验证。 |
| 2026-06-27 | PH3-01 | 完成 AI HTML 响应诊断修复 | 修复/验证/文档同步 | 执行 `npm run progress:build`、`npm run progress:check` 和 `npm run build` 完成收尾验证；用户可重新测试连接，根据新的中文提示修正 Base URL。 |
| 2026-06-27 | PH3-01 | 确认 AI HTML 响应诊断修复边界 | 方案确认/准备修复 | 执行 `npm run progress:build` 同步展示层产物，然后修复 `AiService` 的响应格式诊断并验证。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
