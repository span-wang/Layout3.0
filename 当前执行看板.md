# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第三阶段：AI 与体验 | `M3` 可发布 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |
| `PH3-13` | 资料入库中心与资料生命周期治理 | `P2` | `进行中` | 按 `RAGFLOW_input完整开发方案.md` 完成统一登记、版本与去重、元数据人工确认、持久处理、pending 质量门禁、精确 ID 排序前门禁与 SQLite 返回后二次校验、发布回滚、历史迁移和长期运营；阶段 0 必须先通过 Electron 32 SQLite 与隔离 RAGFlow 合同验证。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH3-07` | 验证 M4 链路 | `P2` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH3-05` | 首次使用引导 | `暂缓` | 首次进入时有可关闭引导流程。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-11 | 返工 | PH3-13C3 主链路首次串联后，最终审查发现若直接按已有绿测收口，仍存在 quality 租约恢复导致 run/job 分裂、执行时只信任工件快照、技术重试沿用旧结果、相同问题无法重跑、配置保存与质量启动竞态、历史 pending 数据集失去正式保护、Renderer 自由文本泄漏，以及旧 worker 在租约接管后覆盖当前 attempt 结果等风险。其中任一问题都可能造成永久锁死、错误隔离或错误进入 pending_publication。 | 已完成审查返工闭环：quality 租约恢复由质量仓储同步收口 run/job 且不污染版本健康；execute 重新读取三工件并逐文件核验；同 resultKey 以当前 attempt 更新并留 before/after 审计；重复问题使用唯一 job 输入键且 run 快照保留 semanticInputHash；配置保存与质量启动串行；保护集合覆盖全部历史 pending 数据集；Renderer 使用受控中文副本；结果写入、scope 解析和终态收口绑定 workerId 与有效租约，旧 worker 接管后不可迟到覆盖，休眠跨 lease 时 runner 会先恢复再继续。综合 registry:test 及双 worker/跨租约定向回归通过，未执行真实 RAGFlow 请求。 |
| 2026-07-10 | 兼容性 | 如果当前 RAGFlow 0.25.0 的 retrieval `document_ids` 在排序后才过滤、零匹配回退全量，或不能与 `status=pending/active` 条件稳定组合，新旧版本并存窗口会发生候选挤占或未发布资料泄漏，SQLite 返回后二次白名单无法单独保证发布连续性。 | PH3-13A 已完成真实隔离复测：原始能力报告 13/15，精确 document_ids 排序前过滤与路线 A 必需项 11/11 通过，两个 document_ids + status 联合限制按真实结果保留。PH3-13B 已把路线 A 三条不变量落为 SQLite 本地代码与测试：只从当前有效发布关系和健康 active 绑定生成精确 dataset/document ID，空集合或映射不完整时本地失败关闭，返回后重新查询当前发布关系并剔除集合外 ID、追加漂移审计。用户已确认 PH3-13C2 只在 Main 专用适配层建立健康 pending 绑定，并使用受保护的专用暂存数据集；现有通用正式检索必须拒绝该数据集，远端 status 继续只用于审计而不是联合门禁。PH3-13C3 已进一步把正式检索保护集合扩展为当前安全配置与 SQLite 中全部 remote_status=pending 数据集的并集；即使配置切换并重启，旧 healthy pending 数据集仍会在 fetch 前被阻断。当前仍不接入生产 active 网关或发布联调，这些归属 PH3-13C4/PH3-13H；document_ids 前置过滤未来失效时降级为物理 staging/live 数据集代次。 |
| 2026-07-10 | 兼容性 | `better-sqlite3` 属于原生模块，普通 Node 可加载不代表 Electron 32 ABI、electron-vite 外部化构建和后续可分发环境能够加载；如果未先验证就锁定依赖，登记库可能在开发环境可用但在构建或安装后启动失败。 | 阶段 0 SQLite PoC 已通过 Electron ABI 128、electron-vite 外部化和 electron-builder `--dir` 真实加载。PH3-13B 已完成正式 schema、迁移备份、恢复重开、未来版本拒写和数据库级测试；PH3-13C1 现已把正式用户登记库接入应用运行时：首次打开独立资料入库中心时惰性开库并恢复过期任务，失败返回中文不可用状态且不阻断普通编辑，应用退出前可靠关闭。schema V2、接收与重启测试已纳入 `npm run registry:test` 并在 Electron 32 下 12/12 通过，主应用构建和真实工作台往返检查通过。完整 NSIS 安装、升级迁移和安装后备份恢复继续留待 PH3-13K，不能用本轮开发运行时接入代替。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-11 | PH3-13 | 加固 Runtime 远端身份串行、历史 pending 保护与质量结果零泄漏 | 配置保存与质量启动串行临界区/旧身份快照竞态阻断/历史 pending 数据集保护集合/配置切换与重启回归/Renderer 稳定质量文案/自由文本零泄漏 | 重新执行综合 registry、src/scripts 全量测试、RAGFlow 合同与元数据测试、构建、git diff 检查、生产依赖高危审计和中文工作台验收。 |
| 2026-07-11 | PH3-13 | 加固质量执行证据、当前 attempt 结果与 worker 租约 fencing | 执行时三工件磁盘复核/resultKey 当前 attempt 更新与审计/相同问题重跑/semanticInputHash/workerId 与有效租约校验/旧 worker 覆盖阻断/跨租约 runner 恢复 | 加固 Runtime 配置保存与质量启动的并发身份边界、历史 pending 数据集重启保护及 Renderer 质量结果自由文本零泄漏。 |
| 2026-07-11 | PH3-13 | 修复 quality 租约重启恢复的运行与任务同步收口 | quality 租约恢复分流/job_id 精确运行解析/取消与满尝试终态同步/可重排队原运行复用/版本健康隔离/重启回归 | 继续加固执行时工件、重试结果、重复运行和 worker 租约 fencing。 |
| 2026-07-11 | PH3-13 | 完成 Runtime 质量创建、配置身份锁与安全 DTO 集成覆盖 | 真实 runtime.startQualityCheck/queued run-job 回读/running 配置身份锁/同身份密钥轮换/内部字段零泄漏/终态动作失败关闭/job_id 精确 run 绑定 | 执行仓库 src/scripts 全量测试、RAGFlow 合同与元数据测试、构建、git diff 检查、生产依赖高危审计和中文工作台验收。 |
| 2026-07-11 | PH3-13 | 统一排队质量任务取消的事务与审计收口 | IntakeStore quality 取消分流/QualityGateRepository.cancelRun 复用/任务与运行双审计/取消后生命周期与无发布回归 | 补 runtime.startQualityCheck 的 queued run/job、安全 DTO 零泄漏、质量运行期间配置身份锁定和运行中取消收口集成测试。 |
| 2026-07-11 | PH3-13 | 完成快速质量门禁执行服务与中文工作台端到端串联 | QualityGateService/ProcessingRunner quality 阶段/runtime 与 IPC/preload/安全 DTO/中文问题表单与质量报告/本地正式通道零泄漏证明/综合登记回归 | 统一 quality 排队取消路径与 QualityGateRepository.cancelRun 的审计语义，补 runtime/IPC 的创建、安全 DTO、配置身份锁定和取消集成测试，再执行 C3 全量门禁与中文工作台验收。 |
| 2026-07-11 | PH3-13 | 完成 Schema V4 与质量运行事务门禁 | SQLite migration/quality run 与 result 仓储/快照与过期门禁/结果不可覆盖/通过阻断失败取消事务/待发布防绕过/综合登记回归 | 完成 QualityGateService 与 ProcessingRunner/runtime/IPC 串联，验证 Top 10、预期证据命中、本地正式零泄漏证明、中文 DTO/UI、取消与配置身份锁定。 |
| 2026-07-11 | PH3-13 | 完成 Main 精确 pending 候选检索层 | RAGFlow retrieval 严格请求与响应合同/quality_run_id scope 双解析/集合外结果审计阻断/取消信号透传/定向测试 | 完成 schema V4 与 quality run/result 仓储，再由质量执行服务把问题证据、候选 Top 10、本地正式门禁证明和原子状态收口串成持久任务。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
