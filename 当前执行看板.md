# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-24A` | 答案显示与试卷解析位置扩展 V1 | `P1` | `进行中` | 自定义语义名称包含 `答案` 或 `解析` 的块，可与内置 `答案 / 解析` 一样参与显示/隐藏与文末统一；`blockquote / 引用容器` 内部的答案解析子块也可参与文末统一与隐藏；画布预览、分页、HTML/PDF、DOCX 和 `.layout` 保存恢复口径一致。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-07 | 范围调整 | 资源库面板若一步扩展为完整文件级资源管理器，会同时牵动本地文件复制/删除、字体授权、图片路径迁移、`.layout` 资源模型、预览资源协议和导出链路，容易造成资源记录与真实文件状态不一致。 | 已完成 `PH3-11-resource-library-panel-v1`：左侧「资源」板块已收口为只消费现有 `LayoutDocument.resources` 的资源查看与图片定位/替换面板；图片可查看缩略图、定位到画布并替换文件路径，字体资源只读展示。本步未做资源入库、拖拽上传、删除磁盘文件、资源重命名、丢失扫描或 `.layout` 顶层结构变更，完整资源管理器继续后续另拆。 |
| 2026-07-07 | 缺陷 | 答案隐藏或文末统一模式会从原始文档块派生出重排后的视图块；如果派生函数每次渲染都返回新的数组或新的嵌套块对象，分页 effect 会把它当成新输入反复写入分页结果，严重时导致 React 渲染循环并表现为画布白屏。 | 已完成 `PH2-24B-answer-hide-white-screen-fix-v1`：`usePagination` 和 `AppShell` 已分别缓存答案隐藏/文末统一模式下的视图块派生结果，文档对象未变化时分页输入和画布 `documentBlocks` 引用保持稳定，避免分页写回后再次触发同一轮分页与测量刷新。本步不改变答案识别范围、复杂容器抽取或导出策略；语义块定向回归、typecheck 与 build 均已通过。 |
| 2026-07-06 | 范围调整 | 把自定义语义与嵌套容器内答案解析并入当前显示/文末汇总规则后，如果没有明确识别边界，就可能把只是在名称上碰巧含有“解析/答案”的普通自定义语义也一起收进来，或者在嵌套容器抽取时破坏原有正文结构与分页口径。 | 已完成 `PH2-24A-answer-display-scope-extension-v1`：名称包含 `答案` 或 `解析` 的自定义语义角色已并入显示/隐藏与文末统一规则；`blockquote / 引用容器` 内部子块已支持递归抽取与隐藏；表格单元格、列表项内部和其他复杂容器仍暂不抽取，继续留待后续任务，以避免一次扩面过大。若后续需要显式配置参与语义或继续扩到更多容器，再单独拆后续任务。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-06 | PH2-07-semantic-block-template | 完成语义块模板 V1 | 文档模型扩展/对象属性接线/预览导出同步/定向回归/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续如需扩到更多块类型、允许块模板改变高度，或开放块模板编辑器，再按 `PH2-07` 后续小步继续拆。 |
| 2026-07-06 | PH2-07-semantic-block-template | 确认语义块模板 V1 边界 | 用户确认开工/设计决策收口/边界登记/风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现块模板文档模型字段、默认模板解析、对象属性面板、预览/导出接线与最小验证。 |
| 2026-07-06 | PH2-05B | 完成分液漏斗与滴定管容器扩展 V1 | 特殊容器状态模型扩展/动态 SVG 渲染/属性区阀门开关/定向测试/类型检查/构建验证/进度同步 | 本步已完成；后续可继续推进特殊容器连接点扩容、更多可编辑器材、动画放液或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认分液漏斗与滴定管容器扩展 V1 边界 | 用户确认开工/特殊容器范围登记/风险范围收口 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展容器状态模型、分液漏斗/滴定管动态 SVG 和右侧属性区的阀门开关。 |
| 2026-07-06 | PH2-05B | 完成化学导管连接点吸附 V1 | 连接点元数据扩展/导管吸附关系建模/端点手柄与高亮/类型检查/生产构建 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续可继续推进自动避障布线、导管部件互吸附、自动对齐或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认化学导管连接点吸附 V1 边界 | 用户确认开工/连接点范围登记/吸附一致性风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展连接点元数据、实现导管线端点吸附与跟随更新。 |
| 2026-07-06 | PH2-05B | 完成化学容器编辑 V1 | 容器元数据扩展/动态 SVG 渲染/属性区接入/类型检查/生产构建 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；后续可继续推进导管连接点吸附、自动对齐或组合成组/解组。 |
| 2026-07-06 | PH2-05B | 确认化学容器编辑 V1 边界 | 用户确认开工/容器属性范围登记/动态渲染风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后扩展可编辑容器元数据、实现组合设计器属性区与动态容器渲染。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
