# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-05B` | 复杂块结构编辑兼容补齐 | `P1` | `进行中` | 补齐列表整体编辑、表格增删行列/表头/对齐、图片路径编辑、引用容器属性、公式可视化渲染编辑、右侧对象属性对子节点展示；明确分隔线与分页符无文字编辑需求。 |
| `PH2-06` | 文本与段落局部样式编辑 | `P0` | `进行中` | 支持字体、字号、颜色、高亮、加粗、斜体、下划线、对齐、缩进、行距、段前后距，并写入模型样式覆盖。 |
| `PH2-09` | 精细分页算法（二次推进） | `P1` | `进行中` | 基于结构化文档模型建立可切换的分页算法注册框架，并在此基础上继续推进孤寡行、表格跨页、表头重复与真实测量分页。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH2-07` | 样式覆盖与模板优先级 | `P1` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |
| `PH2-10` | 表格、图片、公式可视化编辑 | `P1` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |
| `PH2-11` | 目录、大纲与页码回填 | `P1` | `未开始` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-22 | 范围调整 | 当前主链路仍偏纯 Markdown 解析，`ParsedBlock` 多数节点只保留纯文本，后续字体、段落、高亮等可视化编辑缺少稳定保存位置，若继续按原分页深化计划推进会扩大返工面。 | 已将第二阶段默认计划替换为“结构化文档模型优先”，并进一步收口为“Markdown 只导入一次，后续只维护 `.layout` 工程数据”；精细分页二次推进暂缓到模型与样式编辑继续稳定后再推进，具体执行依据见 `结构化文档模型V1方案.md`。 |
| 2026-06-22 | 范围调整 | 第二阶段若直接进入真实分页测量，后续会因模板、块间距、页眉页脚与正文可用区定义尚未收敛而产生较大返工风险。 | 已完成页面配置契约与分页预备能力；该计划已被后续“结构化文档模型优先”的主链路纠偏覆盖。 |
| 2026-06-21 | 风险 | `npm audit` 提示当前 Electron 32 与 Vite 5 依赖链存在安全公告，自动修复会升级到 Electron 42 / Vite 8，属于破坏性主版本变更。 | 暂不执行 `npm audit fix --force`，保持开发文档约定版本推进；在依赖治理或发布前单独评估升级方案。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-23 | PH2-05B | 确认列表整体编辑 V1 小步边界 | 边界归档/继续实施 | 开始修改列表模型操作、store action、右侧对象属性面板和冒烟脚本，接通列表整体编辑 V1。 |
| 2026-06-23 | PH2-05B / PH2-09 | 完成表格表头、列对齐与续表头 V1 | 新增/集成/验证/文档同步 | 继续围绕 `PH2-05B` 和 `PH2-09` 拆分后续能力；优先可选表格列宽/合并单元格、列表整体编辑、或分页代价函数与真实测量分页前置设计。 |
| 2026-06-23 | PH2-05B / PH2-09 | 确认表格表头、列对齐与续表头 V1 小步边界 | 边界归档/继续实施 | 开始实现表格属性模型操作、store 写回、右侧对象属性面板、画布/导出渲染，以及估算分页下的表格行级拆分与续表头冒烟验证。 |
| 2026-06-23 | PH2-09 | 完成优化页尾平衡 V2 | 重构/验证/文档同步 | 继续围绕 `PH2-09` 推进下一步分页基础能力，可优先考虑分页代价函数、脏区重排或真实测量分页前置设计。 |
| 2026-06-23 | PH2-09 | 确认优化页尾平衡 V2 小步边界 | 边界归档/继续实施 | 开始收紧 `estimated-greedy-balance-v2` 的尾块回收判定，并补充更有针对性的冒烟校验。 |
| 2026-06-23 | PH2-09 | 完成页尾平衡 V2 | 新增/重构/验证/文档同步 | 继续围绕 `PH2-09` 推进下一步分页基础能力，优先可选分页代价函数、脏区重排或真实测量分页前置设计。 |
| 2026-06-23 | PH2-09 | 确认页尾平衡 V2 小步边界 | 边界归档/继续实施 | 开始实现新的页尾平衡分页插件，并补充注册与冒烟验证。 |
| 2026-06-23 | PH2-09 | 完成文本行数估算基础校准 V1 | 新增/重构/验证/文档同步 | 继续围绕 `PH2-09` 做下一步分页基础能力推进；优先可选页尾平衡规则、分页代价函数或真实测量分页前置设计。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
