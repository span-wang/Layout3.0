# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-17` | 预览与 PDF 页底偏差修复 V1 | `P1` | `进行中` | 预览页与 PDF 导出页在相同文档、相同模板主题和相同分页结果下，页面底端主要块位置保持一致；至少修复当前表格页底偏差问题，并让导出链路复用与预览一致的页面高度、表格布局、表头高度和行高口径。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH2-01` | 主链路纠偏：确认 Markdown 不是唯一真相源 | `暂缓` | 第二阶段计划已调整为结构化文档模型优先，风险区记录当前纯 Markdown 解析链路对富文本编辑的影响，并沉淀到 `结构化文档模型V1方案.md`。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-28 | 缺陷 | 当前预览页与 PDF 导出页在页面底端存在可见偏差，尤其带表格的页面更明显；若预览和导出继续使用不同的页面高度与表格行高口径，用户会误以为分页结果不可靠。 | 已尝试通过统一页面固定高度和导出表格结构来修复，但用户反馈“更不对了”。当前先回退该轮修复，恢复到修复前状态，再继续排查真实偏差源；暂不宣称该缺陷已解决。 |
| 2026-06-28 | 范围调整 | 当前样式设置只有 `templateId`，若把雪山风格继续作为新模板落地，会把“结构模板”和“视觉主题”耦合在一起，后续每新增一个主题都可能复制多套模板基线并放大维护成本。 | 已完成 `PH2-16`：模板与主题已解耦为 `templateId + themeId` 组合口径，模板继续负责结构与排版基线，主题独立为 `themeId` 负责视觉皮肤。当前已落地默认主题与 `雪山静境`，并让 `.layout` 保存恢复、隐藏测量层、画布预览和 HTML/PDF 导出共同消费同一套主题变量，避免后续新增主题时复制整套模板。 |
| 2026-06-28 | 缺陷 | 自定义语法映射重新打开 `.layout` 文件后不在「语法映射」面板显示，但已解析到正文模型中的样式仍然生效，容易让用户误以为规则丢失或无法继续维护。 | 已完成 `PH2-13-custom-mapping-reopen-display-fix-v1` 小修复：语法映射配置会在新建、Markdown 导入、`.layout` 保存/打开、草稿恢复、store 写回和面板展示入口统一规范化；默认映射补齐、自定义映射保留，历史 `metadata.syntaxMappingConfig` 会迁移到 `meta.syntaxMappingConfig`。已通过保存重开和旧字段迁移回归验证；本步不尝试从旧文件已解析样式中反推原始正则。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-28 | PH2-17 | 回退页底偏差修复 V1 | 用户反馈/回退修复/恢复基线 | 撤销 `src/services/exportHtml.ts` 与 `scripts/m2-smoke.ts` 中本轮页底偏差修复相关改动，随后执行类型检查、冒烟、构建和进度校验。 |
| 2026-06-28 | PH2-17 | 完成预览与 PDF 页底偏差修复 V1 | 缺陷修复/导出对齐/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户重新导出当前文档后，可对比预览页与 PDF 页底的表格位置是否已经对齐。 |
| 2026-06-28 | PH2-17 | 确认预览与 PDF 页底偏差修复 V1 边界 | 缺陷确认/边界登记/风险同步/准备修复 | 执行 `npm run progress:build` 同步展示层产物，然后开始统一导出页与预览页的页面高度、表格布局和默认行高口径。 |
| 2026-06-28 | PH2-16 | 完成模板风格主题组合应用 V1 | 功能增强/样式契约/导出对齐/验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户刷新应用后可在「页面设置 - 模板起点」中选择 `雪山静境`，并将其套用到当前模板。 |
| 2026-06-28 | PH3-09 | 完成 LaTeX 字体颜色语法解析 | 新增/集成/验证 | 用户可导入包含 \color{red}{文字} 语法的 Markdown 验证颜色解析效果；其他颜色语法、背景色、颜色配置 UI 后续单独任务推进。 |
| 2026-06-28 | PH3-09 | 补记 LaTeX 字体颜色语法解析边界 | 治理修复/日志补齐 | 继续按当前优先级推进已确认的小步；本条补记只用于修复进度数据一致性，不代表 `PH3-09` 已完成。 |
| 2026-06-28 | PH2-16 | 确认模板风格主题组合应用 V1 边界 | 方案确认/边界登记/风险同步/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现 `themeId`、主题选择 UI、雪山主题样式契约和导出口径。 |
| 2026-06-28 | PH2-10 | 完成公式根号渲染连续性修复 | 缺陷修复/公式渲染/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户刷新应用后可重新查看二次求根公式，确认根号连续且粗细一致。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
