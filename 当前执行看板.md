# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-01` | 主链路纠偏：确认 Markdown 不是唯一真相源 | `P0` | `进行中` | 第二阶段计划已调整为结构化文档模型优先，风险区记录当前纯 Markdown 解析链路对富文本编辑的影响，并沉淀到 `结构化文档模型V1方案.md`。 |
| `PH2-21F` | DOCX 方程组大括号兼容 V1 | `P1` | `进行中` | DOCX 公式导出支持二元一次方程组和分段函数这类“左大括号 + 多行”结构；至少覆盖 `\begin{cases}...\end{cases}`、`\left\{\begin{aligned}...\end{aligned}\right.` 与 `\left\{\begin{array}...\end{array}\right.`；Word/WPS 中应优先输出可伸缩的大括号和多行 Office Math 结构，而不是整条退回 LaTeX 源码。 |
| `PH2-24A` | 答案显示与试卷解析位置扩展 V1 | `P1` | `进行中` | 自定义语义名称包含 `答案` 或 `解析` 的块，可与内置 `答案 / 解析` 一样参与显示/隐藏与文末统一；`blockquote / 引用容器` 内部的答案解析子块也可参与文末统一与隐藏；画布预览、分页、HTML/PDF、DOCX 和 `.layout` 保存恢复口径一致。 |
| `PH3-12` | 个人知识库接入 V1 | `P2` | `进行中` | 左侧新增“个人知识库”菜单与面板，可配置并测试 RAGFlow 与 Open Notebook 连接；RAGFlow 可列出数据集并检索知识片段；AI 生成可结合知识库片段生成知识清单、教辅讲义、同步练习和单元试卷草稿。 |

## 待确认的下一步

| 任务 ID | 步骤 | 优先级 | 当前状态 | 说明 |
|---|---|---|---|---|
| `PH2-01` | 主链路纠偏：确认 Markdown 不是唯一真相源 | `P0` | `进行中` | 按 AGENTS 约定，开始改代码前仍需先确认该任务的执行方案与细节。 |

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-08 | 范围调整 | AI 内容生成新增“要求描述”若同步扩成完整提示词模板系统、变量字段库、题型比例、难度梯度和专业出题参数面板，会把当前单字段补强扩大到提示词编排、记录筛选、知识库检索和多任务模板治理，难以形成一个可快速验收的小步。 | 已完成 `PH3-12-generate-requirement-description-v1` 最小闭环：教育内容生成模式已新增可选“要求描述”，并同步进入 `GenerateOptions`、提示词、AI 生成记录落盘与记录详情展示；小红书模式、完整模板系统、模型 Provider、个人知识库检索和出题高级参数均未在本步扩展。 |
| 2026-07-08 | 范围调整 | 导出检查如果一步扩成“真实磁盘资源扫描 + 自动修复 + 强制拦截导出 + 全格式高保真诊断”，会同时牵动资源协议、文件系统访问、PDF / DOCX 导出链路、对象定位和交互确认流程，容易让当前最需要的“先看见风险”能力迟迟不能交付。 | 已完成 `PH4-02-export-check-panel-v1`：当前已落地本地规则检查最小闭环，覆盖分页 warnings、图片资源基础风险、目录风险、答案视图提示、页面背景提示和 DOCX 兼容性提示，并在导出前复用同一套结果做中文提醒；自动修复、深度扫描和导出阻断继续后续单拆。 |
| 2026-07-07 | 返工 | 界面刷新、恢复草稿或重新导入文档后，若 `CanvasPane` 中的编辑态、浮动工具条、公式编辑遮罩或拖拽态继续跨文档残留，或者透明弹层在不可见时仍保留点击命中，就会把顶栏、左侧、右侧和 AI / 知识库等输入框的点击全部吞掉，形成“所有输入框都无法点击使用”的假禁用状态。 | 已完成 `PH2-12-input-click-recovery-v1`：当前已用 `documentEpoch` 驱动 `CanvasPane` 和 `AppShell` 在文档切换时清理旧交互态，覆盖编辑态、公式编辑器、浮动工具条、右键菜单和图片 / 表格拖拽草稿；同时把公式编辑遮罩默认态改为 `pointer-events: none`，只在可见态接收点击。`npm run typecheck` 与 `npm run build` 已通过；本轮未补完整 Electron 文件导入自动化回归，仍建议继续做真实桌面复测。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-07 | PH2-07-semantic-block-template | 完成语义标签块内胶囊返修 V1 | 语义标签 DOM 改造/测量节点收口/列表 wrapper 补齐/导出同步/回归验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需继续微调胶囊尺寸、不同主题下的细节，继续归属 `PH2-07` 视觉收口小步。 |
| 2026-07-07 | PH2-07-semantic-block-template | 确认语义标签块内胶囊返修 V1 边界 | 用户确认返修/范围收口/DOM 测量约束登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现块内胶囊语义标签、列表显式标签渲染和隐藏测量层 / 导出同步收口。 |
| 2026-07-07 | PH2-10A | 确认图片纵向拖动后重锚定返修 V1 边界 | 用户确认返工/范围收口/风险登记 | 开始实现图片拖拽释放后的顶层重锚定、来源块 ID 暴露和最小验证。 |
| 2026-07-07 | PH3-12 | 收口个人知识库面板文案 | 界面文案精简/描述性文字移除/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；后续如需继续压缩其他面板文案，可按同样方式继续收口。 |
| 2026-07-07 | PH3-12 | 完成个人知识库接入 V1 第一轮实现 | 左侧菜单接入/知识库配置存储/RAGFlow 连接器/AI 生成链路接线/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层；如果本机 Docker / 知识库服务恢复，再继续做 `PH3-12` 的真实接口联调和端到端生成验证。 |
| 2026-07-07 | PH3-12 | 确认个人知识库接入 V1 边界 | 用户确认开工/范围收口/风险登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现左侧“个人知识库”菜单、知识库面板、主进程知识库请求与 RAGFlow 检索接入。 |
| 2026-07-07 | PH2-10A | 完成图片自由拖动与复杂绕排 V1 | 画布拖动预览/隐藏测量层共享正文流/导出样式同步/类型检查/定向验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续如需继续贴近 Word，可优先拆透明轮廓紧密绕排、DOCX 浮动对象或跨页自由定位。 |
| 2026-07-07 | PH2-10A | 确认图片自由拖动与复杂绕排 V1 边界 | 用户确认开工/范围收口/风险登记 | 执行 `npm run progress:build` 与 `npm run progress:check`；随后开始实现图片自由拖动、页面级浮动绕排、分页估算和 HTML/PDF 导出联动。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
