# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-07-custom-semantic-keyword-rules` | 自定义语义块规则 V1：前端自定义语义角色与关键词批量应用 | `P1` | `进行中` | 用户可在前端创建自定义语义角色，配置 `【答案解析】` 等关键词前缀规则，并对当前文档扫描后批量写入 `LayoutBlock.semantic`；本步仅做前缀匹配和块级语义，不扩展复杂正则、深层列表/表格单元格语义或分页规则。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-07-05 | 返工 | V1 化学图式虽然打通了插入、保存和导出链路，但素材表现偏单个仪器图标，缺少教材/试卷常用的组合装置、导管、橡胶塞、液面、箭头和实验标签，用户反馈“太简陋”，无法满足真实教学资源使用预期。 | 已确认按 `PH2-05B-chemistry-apparatus-template-gallery-v2` 返工：把素材升级为教材式组合装置模板，并把顶栏下拉改为带缩略图的小型素材面板；仍坚持项目原创 SVG 与现有图片块链路，不抓取版权不明教学图片。 |
| 2026-07-05 | 范围调整 | 化学实验装置图式若直接使用国内教材、教辅、题库或商业素材站图片，会带来版权授权不清与后续产品内置不可控风险；若同一步接入完整绘图编辑器，又会扩大到素材管理、图形组合、标签编辑、连接点和导出多条链路。 | 已完成 `PH2-05B-chemistry-apparatus-svg-library-v1`：第一版仅接入项目原创黑白 SVG 线稿，并复用现有图片块插入链路；未抓取国内教学资源，未内置授权不明素材，未接完整化学绘图编辑器。后续如引入外部开源/商业素材包，需另做授权台账和素材库治理。 |
| 2026-07-05 | 体验缺陷 | 顶端字体字号工具把“当前选区即时修改”和“全文字 / H1 / 段落 / 列表 / 表格批量规则应用”混在同一组控件中，用户需要先理解范围下拉和应用按钮，和 Word 中“选中什么就改什么”的常见心智不一致，导致字体调整和各类型字号应用显得别扭、不方便。 | 已完成 `PH2-14-word-like-font-size-application-v1` 最小交互优化后，用户进一步指出批量应用字体和批量应用字号仍不能共用同一个应用按钮，否则会把标题、段落等不同类型字号一起写成同一个值。已完成 `PH2-14-font-family-font-size-separate-batch-actions-v1`：批量范围共用，但 `应用字体` 只写 `fontFamily`，`应用字号` 只写 `fontSize`；`typecheck` 与 `build` 均已通过。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-05 | PH2-07-custom-semantic-keyword-rules | 补记自定义语义块规则 V1 开工边界日志 | 进度一致性补登记/不改代码/不改任务状态 | 继续保持 `PH2-07-custom-semantic-keyword-rules` 为进行中；后续完成实际功能后再补完成日志和验证。 |
| 2026-07-05 | PH2-06 | 确认浮动工具条字体字号调整 V1 边界 | 用户确认开工/边界登记/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后实现浮动工具条字体下拉、字号调整和紧凑布局，并运行类型检查与构建验证。 |
| 2026-07-05 | PH2-05B | 确认教材式化学实验装置模板图库 V2 边界 | 用户确认返工/体验问题定位/边界登记/风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后重做教材式化学装置 SVG 模板库，并把顶栏入口升级为缩略图素材面板。 |
| 2026-07-05 | PH2-05B | 完成化学实验图式 SVG 素材插入 V1 | 内置 SVG 素材库新增/顶栏插入入口接入/类型检查/构建验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续若继续化学图式，可优先拆完整素材面板、外部授权素材台账或化学装置拖拽组合编辑器。 |
| 2026-07-05 | PH2-07-ai-semantic-prompt | 完成 AI 默认生成语义角色前缀 V1 | 默认提示词扩展/最小测试补齐/类型检查/进度同步 | 执行 `npm run build`、`npm run progress:build` 与 `npm run progress:check` 完成构建和进度展示同步；后续如需进一步区分来源，可单独扩展 AI 插入链路把语义来源标记为 `ai`。 |
| 2026-07-05 | PH2-05B | 确认化学实验图式 SVG 素材插入 V1 边界 | 用户确认开工/素材授权范围收口/边界登记/风险记录 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；随后新增项目原创化学 SVG 图式库和顶栏插入入口，并运行类型检查与构建验证。 |
| 2026-07-05 | PH2-07-ai-semantic-prompt | 确认 AI 默认生成语义角色前缀 V1 边界 | 用户确认/边界登记/准备修改默认提示词 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档，然后修改 AI 教育生成默认提示词并补充最小验证。 |
| 2026-07-05 | PH2-07-semantic-role-model | 完成语义块基础 V1 | 模型扩展/Markdown 导入/UI 写回/预览导出标记/回归验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check`；后续若继续语义块，可优先做关键词批量识别、语义角色样式绑定或答案解析/提示等结构化容器块。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
