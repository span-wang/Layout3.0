# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M4` AI 就绪 | 进行中 / 进行中 |

## 正在进行

当前没有显式标记为 `进行中` 的任务。

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-29 | 返工 | `PH2-19-column-css-overflow-clip-fix-v1` 通过多栏容器硬裁切阻止第三栏露出，但会把放不下的内容隐藏掉，违反排版必须完整显示内容的验收标准。 | 已完成 `PH2-19-column-overflow-pagination-fix-v2` 返工：撤掉 V1 中依赖 `overflow: hidden`、`max-height` 和 `column-fill: auto` 裁掉内容的修复思路；分页测试算法1多栏文本拆分已改为使用当前可用栏高度，当前页片段放入后重建栏位状态，剩余文本继续进入后续栏或下一页；`smoke:m2` 已覆盖双栏长段落完整拼回原文，避免内容丢失。 |
| 2026-06-29 | 缺陷 | 字体上传后依赖当前文档 `resources` 展示，重新导入或打开另一个文档时资源列表被新文档替换，导致工作区已上传字体从顶端工具栏字体下拉中消失。 | 已完成 `PH2-06A-workspace-font-visibility-fix-v1`：工作区字体在打开、导入或新建文档后会合并回当前文档资源，顶端字体下拉、画布预览和 HTML/PDF 导出继续消费同一套字体资源；字体导入返回字段已统一；`typecheck`、`smoke:m2` 和 `build` 已通过。本步未扩展字体管理器、全局字体库或分页算法。 |
| 2026-06-28 | 缺陷 | 手绘主题的标题装饰存在额外占位高度和缩进，但当前分页测量/估算缺少统一的主题装饰占位契约，后续更多主题如果继续用 CSS 装饰也可能让分页结果低估块高或切页不稳定。 | 已完成 `PH2-09-theme-decoration-measure-contract-v1`：主题装饰占位已进入 `ThemeLayoutMetrics` 和 `ResolvedStyleContract`，手绘主题 H1/H2/H3 占高装饰通过统一 CSS 变量进入画布预览、隐藏测量层和 HTML/PDF 导出；分页估算和分页测试算法1会计入这些占位，并让测量缓存随主题布局指标变化失效。后续新增占高装饰主题必须复用该契约。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-07-01 | PH2-09 | 完成分页测试算法1页底安全边界修复 V1 | 分页算法修复/回归断言补充/验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续若继续分页质量深化，可单独推进真实行级分页或孤寡行控制。 |
| 2026-07-01 | PH2-09 | 确认分页测试算法1页底安全边界修复 V1 边界 | 问题排查/方案确认/边界登记/风险更新 | 执行 `npm run progress:build` 同步展示层产物，然后开始修复 `max-fill-v1` 页底安全边界与高度乐观判定，并补充回归验证。 |
| 2026-07-01 | PH2-09 | 新增离屏渲染精确测量分页算法 offscreen-measure-v1 | 前端实现/算法注册/类型修复/验证 | 等待 smoke:m2 测试完成，验证离屏测量分页算法正常工作；后续可继续完善离屏测量能力或进入其他 PH2-09 子步。 |
| 2026-07-01 | PH2-09 | 确认表格续页重复表头 V1 边界 | 方案确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现 `dom-measure-v1` 表格续页重复表头和对应回归验证。 |
| 2026-07-01 | PH2-09 | 完成表格按行真实测量 V1 | 前端实现/运行时测量/表格分页接入/回归验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续若继续推进，可进入表头重复或多栏真实测量子步。 |
| 2026-07-01 | PH2-09 | 确认表格按行真实测量 V1 边界 | 方案确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现表格行真实测量状态、隐藏测量层回填和 `dom-measure-v1` 表格分页消费。 |
| 2026-07-01 | PH2-09 | 完成超长列表项真实测量 V1 | 前端实现/列表深拆/运行时测量/回归验证/进度同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步并校验展示文档；后续若继续推进，可进入表格按行真实测量深化或多栏真实测量子步。 |
| 2026-07-01 | PH2-09 | 确认超长列表项真实测量 V1 边界 | 方案确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现列表项片段测量、隐藏测量层回填和 `dom-measure-v1` 列表项分页消费。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
