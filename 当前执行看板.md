# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-06` | 文本与段落局部样式编辑 | `P0` | `进行中` | 支持字体、字号、颜色、高亮、加粗、斜体、下划线、对齐、缩进、行距、段前后距，并写入模型样式覆盖。 |
| `PH2-09` | 精细分页算法（二次推进） | `P1` | `进行中` | 基于结构化文档模型建立可切换的分页算法注册框架，并在此基础上继续推进孤寡行、表格跨页、表头重复与真实测量分页。 |
| `PH2-10` | 表格、图片、公式可视化编辑 | `P1` | `进行中` | 支持表格调整、图片环绕、公式渲染与对应模型属性保存。 |
| `PH2-11` | 目录、大纲与页码回填 | `P1` | `进行中` | 基于文档模型生成目录和大纲，分页完成后可回填页码并支持跳转。 |
| `PH2-12` | 验证 M2 链路 | `P1` | `进行中` | Markdown 输入、模型保存、画布样式编辑、分页预览、模板应用、导出保存可稳定串联。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

当前阶段没有标记为 `暂缓` 或 `阻塞` 的任务。

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-25 | 返工 | 图片环绕上一版采用“图片块 + 紧邻段落”的局部绕排，仍然受块级布局限制，图片会独占纵向高度，和 Word 四周型/紧密型绕排不一致。 | 已按用户确认切换为页面级浮动绕排返工：四周型/紧密型图片作为同页浮动对象，不再单独累加图片块高度；后续多个正文块持续绕开图片直到图片底部。嵌入型和上下型继续保持普通图片块占高。 |
| 2026-06-22 | 范围调整 | 当前主链路仍偏纯 Markdown 解析，`ParsedBlock` 多数节点只保留纯文本，后续字体、段落、高亮等可视化编辑缺少稳定保存位置，若继续按原分页深化计划推进会扩大返工面。 | 已将第二阶段默认计划替换为“结构化文档模型优先”，并进一步收口为“Markdown 只导入一次，后续只维护 `.layout` 工程数据”；精细分页二次推进暂缓到模型与样式编辑继续稳定后再推进，具体执行依据见 `结构化文档模型V1方案.md`。 |
| 2026-06-22 | 范围调整 | 第二阶段若直接进入真实分页测量，后续会因模板、块间距、页眉页脚与正文可用区定义尚未收敛而产生较大返工风险。 | 已完成页面配置契约与分页预备能力；该计划已被后续“结构化文档模型优先”的主链路纠偏覆盖。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-26 | PH2-09 | 修复分页测试算法1表格行整行翻页导致的大空白 | 修复/优化/验证/文档同步 | 继续在 `PH2-09` 后续小步中推进合并单元格跨页、真实测量分页、表格行内复杂内容拆分和更接近 Word 的表格分页细节。 |
| 2026-06-26 | PH2-09 | 修复分页测试算法1列表整块翻页导致的大空白 | 修复/优化/验证/文档同步 | 继续在 `PH2-09` 后续小步中推进列表项内部长文本跨页、多级列表更精细跨页、孤儿行/寡妇行控制和真实测量分页。 |
| 2026-06-26 | PH2-09 | 优化分页测试算法1文本分割顺序与格式保留 | 修复/优化/验证/文档同步 | 继续在 `PH2-09` 后续小步中推进孤儿行/寡妇行控制、真实测量分页、表格复杂跨页和更精细的分页质量优化。 |
| 2026-06-26 | PH2-09 | 优化分页测试算法1文本分割逻辑 | 新增/优化/验证 | 继续测试分页效果，如果仍有分割不准确问题，继续优化 `computeOptimalTextSplit` 的边界处理逻辑。 |
| 2026-06-25 | PH2-10 | 确认图片环绕页面级浮动绕排返工 | 返工/风险登记/继续实施 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物，然后返工 CanvasPane 画布预览、分页估算、HTML/PDF 导出和 M2 冒烟验证。 |
| 2026-06-25 | PH2-10 | 完成图片环绕最终形态 | 新增/兼容/集成/验证/文档同步 | 后续如需继续贴近 Word，可在 `PH2-10` / `PH2-09` 中拆分透明像素轮廓紧密绕排、真正段落内 inline 图片、跨页自由定位和真实测量分页联动。 |
| 2026-06-25 | PH2-04 | 完成可扩展语法映射注册表 V1 实现 | 新增/验证 | 前端语法映射 UI 配置可后续在 `PH2-06` 或样式系统相关任务中继续扩展；更多 LaTeX 语法支持（如 \textbf、\textit）可在 `syntax-mappings.ts` 的 `SYNTAX_MAPPINGS` 数组中继续扩展。 |
| 2026-06-25 | PH2-04 | 确认可扩展语法映射注册表 V1 边界 | 边界归档/继续实施 | 开始实现语法映射注册表、remark 插件、fromMarkdown.ts underline 映射，运行 typecheck / build / smoke:m2 验证。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
