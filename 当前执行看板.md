# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

当前没有显式标记为 `进行中` 的任务。

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH2-01` | 主链路纠偏：确认 Markdown 不是唯一真相源 | `暂缓` | 第二阶段计划已调整为结构化文档模型优先，风险区记录当前纯 Markdown 解析链路对富文本编辑的影响，并沉淀到 `结构化文档模型V1方案.md`。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-28 | 缺陷 | 自定义语法映射重新打开 `.layout` 文件后不在「语法映射」面板显示，但已解析到正文模型中的样式仍然生效，容易让用户误以为规则丢失或无法继续维护。 | 已完成 `PH2-13-custom-mapping-reopen-display-fix-v1` 小修复：语法映射配置会在新建、Markdown 导入、`.layout` 保存/打开、草稿恢复、store 写回和面板展示入口统一规范化；默认映射补齐、自定义映射保留，历史 `metadata.syntaxMappingConfig` 会迁移到 `meta.syntaxMappingConfig`。已通过保存重开和旧字段迁移回归验证；本步不尝试从旧文件已解析样式中反推原始正则。 |
| 2026-06-28 | 缺陷 | 语法映射 AI 正则识别遇到 LaTeX 示例 `$ \underline{\text{文字}} $` 时，模型可能返回非严格 JSON 或未完整转义的反斜杠正则，导致前端只提示“AI 返回内容不是有效 JSON”，用户无法采用候选规则。 | 已完成 `PH2-13-ai-regex-json-tolerance-v1` 小修复：`AiService` 现在可从非严格 JSON/Markdown 包裹文本中提取规则字段，并会对反斜杠 pattern 做候选修复；`$...$` 示例会提供核心 LaTeX 语法给 AI，同时 `inlineMath` 命中映射时可转入现有 TextMark 链路；`$ \underline{\text{文字}} $` 已通过本地回归验证。 |
| 2026-06-28 | 范围调整 | AI 设置从单配置扩展为多配置后，生成、优化、检查、语法映射 AI 正则识别都需要明确读取各自分配的配置；若迁移规则不清晰，旧用户保存的单配置可能在升级后暂时不可用。 | 已完成 `PH3-08`：AI 设置页支持多套本机配置档案和任务功能分配，生成、优化、检查、AI 正则识别会分别读取对应配置；旧单配置会自动迁移为“默认配置”，并默认分配给所有任务。API Key 加密、云同步、提示词模板和费用统计仍按边界延后处理。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-28 | PH2-10 | 完成公式根号渲染连续性修复 | 缺陷修复/公式渲染/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户刷新应用后可重新查看二次求根公式，确认根号连续且粗细一致。 |
| 2026-06-28 | PH2-10 | 确认公式根号渲染连续性修复边界 | 缺陷反馈/边界登记/准备修复 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物，然后开始修改公式渲染输出模式并验证根号显示。 |
| 2026-06-28 | PH2-15 | 完成页面设置二级菜单显示返修 | 缺陷修复/样式收敛/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户刷新应用后可在「页面设置」顶部看到两列二级菜单入口。 |
| 2026-06-28 | PH2-15 | 确认页面设置二级菜单显示返修边界 | 缺陷反馈/边界登记/准备修复 | 开始最小化修复 `src/styles.css` 中页面设置二级菜单的宽度、盒模型与横向溢出，再运行类型检查、构建和进度校验。 |
| 2026-06-28 | PH2-15 | 完成块排版参数预设 V1 | 功能增强/样式契约/预设管理/回归修复/验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户可在「页面设置-块排版」中查看二级菜单和预设管理入口。 |
| 2026-06-28 | PH2-15 | 确认块排版参数预设 V1 开发边界 | 需求确认/边界登记/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后开始扩展 `StyleSettings`、样式契约、右侧面板和 M2 冒烟验证。 |
| 2026-06-28 | PH2-13 | 完成自定义映射重开后显示修复 V1 | 缺陷修复/迁移兼容/回归验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示文档；用户可新增一条自定义映射、保存 `.layout`、重新打开后确认该规则仍显示在「语法映射」面板。 |
| 2026-06-28 | PH2-13 | 确认自定义映射重开后显示修复边界 | 缺陷确认/边界登记/风险同步/准备修复 | 执行 `npm run progress:build` 同步展示层产物，然后开始实现语法映射配置规范化、旧字段兼容和保存重开回归验证。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
