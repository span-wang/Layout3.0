# 当前执行看板

> 本文件由 `npm run progress:build` 自动生成，请不要直接手改。

## 当前焦点

| 项目当前阶段 | 当前里程碑 | 当前状态 |
|---|---|---|
| 第二阶段：主链路纠偏与排版深化 | `M2` 排版可用 | 进行中 / 进行中 |

## 正在进行

| 任务 ID | 步骤 | 优先级 | 状态 | 完成标准 |
|---|---|---|---|---|
| `PH2-14` | 字体字号批量快捷应用 V1 | `P1` | `进行中` | 支持在顶端快捷格式栏按全文字、H1、H2、H3、H4、段落、列表、表格范围快速设置字体和字号；全文字作为低优先级基线，块类型设置可覆盖全文设置，单个节点或选区局部样式继续保持最高优先级，并被预览、分页估算与 HTML/PDF 导出共同消费。 |

## 待确认的下一步

当前阶段没有可继续推进的开放任务。

## 已确认边界，待开始

当前没有已确认边界的待开发任务。

## 暂缓与阻塞

| 任务 ID | 步骤 | 状态 | 完成标准 |
|---|---|---|---|
| `PH2-09` | 精细分页算法（二次推进） | `暂缓` | 基于结构化文档模型建立可切换的分页算法注册框架，并在此基础上继续推进孤寡行、表格跨页、表头重复与真实测量分页。 |

## 最近风险

| 日期 | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| 2026-06-27 | 缺陷 | 多行段落在原位富文本编辑态应用文字/字体格式时，选区偏移统计没有稳定计入 `<br>` 对应的换行字符，可能导致末尾若干字没有进入格式应用范围，并在模型中形成未格式化的独立 `TextRun/span`。 | 已完成修复：`CanvasPane.tsx` 中选区点查找和偏移测量已统一把 `<br>` 与块级换行按 1 个字符计入，和 `TextRun` 的 `\n` 口径一致；`smoke:m2` 已补充多行段落完整选区尾部格式回归验证。 |
| 2026-06-26 | 范围调整 | 用户明确要求后续不要再继续分页算法开发，当前开发重心需要从 `PH2-09` 切回非分页产品功能收尾。 | 已将 `PH2-09` 状态调整为 `暂缓`，后续默认不再把分页算法作为下一步推荐；除非用户重新确认恢复分页方向，否则优先推进 `PH2-10`、`PH2-11` 或 `PH2-12` 的非分页收尾任务。 |
| 2026-06-25 | 返工 | 图片环绕上一版采用“图片块 + 紧邻段落”的局部绕排，仍然受块级布局限制，图片会独占纵向高度，和 Word 四周型/紧密型绕排不一致。 | 已按用户确认切换为页面级浮动绕排返工：四周型/紧密型图片作为同页浮动对象，不再单独累加图片块高度；后续多个正文块持续绕开图片直到图片底部。嵌入型和上下型继续保持普通图片块占高。 |

## 最近进度日志

| 日期 | 任务 ID | 步骤 | 动作 | 下一步 |
|---|---|---|---|---|
| 2026-06-27 | PH3-01 | 完成 AI 面板基础结构实现 | 实现/验证 | AI 面板基础结构完成，待用户配置 AI 服务后进行实际功能测试。后续可继续增强：悬浮菜单润色入口、AI 内容注释标记、完整错误处理与重试机制等。 |
| 2026-06-27 | PH3-01 | 确认 AI 面板基础结构开发方案 | 方案确认/准备实现 | 开始实现 AI 类型定义、AiService 核心服务、AI Store 状态管理和 AI 面板 UI 组件。 |
| 2026-06-27 | PH2-14 | 完成字体字号批量快捷应用 V1 | 新增/集成/验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物；后续可进入 `PH2-12` 做 M2 桌面链路人工验收，重点回归顶端字体字号批量应用与局部样式覆盖优先级。 |
| 2026-06-27 | PH2-13 | 添加自定义语法映射入口 | 功能增强 | 刷新应用页面，测试添加自定义语法映射功能。 |
| 2026-06-27 | PH2-13 | 补充语法映射配置面板样式 | 完善样式 | 刷新应用页面，查看语法映射面板的样式效果。 |
| 2026-06-27 | PH2-14 | 确认字体字号批量快捷应用 V1 开发边界 | 边界确认/准备实现 | 执行 `npm run progress:build` 同步展示层产物，然后实现字体字号范围规则、顶端快捷入口、预览/分页/导出消费与 `smoke:m2` 验证。 |
| 2026-06-27 | PH2-06 | 完成多行段落格式尾字漏应用修复 | 修复/验证/文档同步 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物；后续可进入 `PH2-12` 做 M2 桌面链路人工验收，重点回归多行段落选区和格式工具条。 |
| 2026-06-27 | PH2-13 | 完成前端可配置语法映射系统 V1 端到端集成 | 新增/集成/验证 | 执行 `npm run progress:build` 与 `npm run progress:check` 同步展示层产物，然后执行 `npm run smoke:m2` 确认整体链路未回退。 |

## 使用规则

- 真相源在 `progress/` 目录，不在本文件。
- 完成一步后，先更新任务卡、风险或日志，再执行 `npm run progress:build`。
- 提交前可执行 `npm run progress:check`，确保生成文件没有漏更新。
